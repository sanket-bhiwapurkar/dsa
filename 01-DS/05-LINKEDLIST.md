# LinkedList

for implementation, see `05.1-LINKEDLIST.js`<br>
this includes:

- Node class
- LinkedList class
- toArray(),
- push(),
- unshift(val),
- pop(),
- shift(),
- insert(index, val),
- remove(index),
- slice(startIndex, endIndex),
- traverseToIndex(index)
